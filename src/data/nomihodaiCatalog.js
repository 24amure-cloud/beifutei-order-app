/**
 * 飲み放題専用メニュー（通常ドリンクとは完全分離）
 * マスター編集・実行時データは NomihodaiCatalogProvider / localStorage を参照。
 * ここは後方互換のため初期データと同名エクスポートのみ。
 */
export { DEFAULT_NOMIHODAI_CATALOG as NOMIHODAI_CATALOG } from './defaultNomihodaiCatalog.js';
